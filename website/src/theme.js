import { createTheme } from '@mui/material/styles'

const theme = createTheme({
	palette: {
		primary: {
			main: '#5865F2',
		},
		background: {
			paper: '#2C2F33',
			default: '#23272A',
		},
		text: {
			primary: '#ffffff',
		},
	},
})

export default theme
